import { FilterByCardTypePipe } from './filter-by-card-type.pipe';

describe('FilterByCardTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCardTypePipe();
    expect(pipe).toBeTruthy();
  });
});
